# AutoGen + Chengeta + Ollama

Run:

```bash
uv run python -m cookbook.autogen.agent
```

Uses `autogen-agentchat` style assistant wrapped with `AutoGenCacheAdapter`.

Prerequisites:

```bash
uv pip install autogen-agentchat autogen-ext tiktoken
```
